version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - export SERVER_URL=$SERVER_URL
        - npm run build:production
  artifacts:
    baseDirectory: dist/angular-websocket-app
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**

